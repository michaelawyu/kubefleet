---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.0
  name: dynamicresourceclasses.placement.kubernetes-fleet.io
spec:
  group: placement.kubernetes-fleet.io
  names:
    categories:
    - fleet
    - fleet-placement
    kind: DynamicResourceClass
    listKind: DynamicResourceClassList
    plural: dynamicresourceclasses
    singular: dynamicresourceclass
  scope: Cluster
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec is the desired configuration of a DynamicResourceClass.
            properties:
              selector:
                description: |-
                  Selector is a DynamicResourceClassSelector that help select resources
                  belonging to this DynamicResourceClass.
                properties:
                  matchAttributes:
                    additionalProperties:
                      type: string
                    description: |-
                      MatchAttributes is a list of string-based key/value pairs that are used to match against
                      resource attributes.
                      TO-DO (chenyu1): switch to CEL expressions for better flexibility (see KEP 4381's DeviceClass API).
                    type: object
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
