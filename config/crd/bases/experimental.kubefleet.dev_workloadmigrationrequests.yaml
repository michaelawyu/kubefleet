---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: workloadmigrationrequests.experimental.kubefleet.dev
spec:
  group: experimental.kubefleet.dev
  names:
    categories:
    - kubefleet
    - kubefleet-experimental
    kind: WorkloadMigrationRequest
    listKind: WorkloadMigrationRequestList
    plural: workloadmigrationrequests
    shortNames:
    - wkmigreq
    singular: workloadmigrationrequest
  scope: Cluster
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: The spec of a workload migration request.
            properties:
              failurePolicy:
                description: |-
                  The failure policy of this migration request. It helps KubeFleet to stop the migration attempt
                  when there are too many failures, so as to keep the impact radius within control.

                  This field is immutable after creation.
                properties:
                  maxFailureCount:
                    default: 1
                    description: |-
                      The maximum number of failed migration attempts allowed before KubeFleet stops the migration.
                      A migration attempt is the replacement of one specific workload from one specific from cluster to one
                      specific to cluster.

                      If this threshold is reached, KubeFleet will suspend the migration process. Delete the request
                      to accept the current state, or roll back the request to restore to the original state.

                      The default value is 1.

                      This field is immutable after creation.
                    format: int32
                    minimum: 1
                    type: integer
                    x-kubernetes-validations:
                    - message: the maxFailureCount field is immutable
                      rule: self == oldSelf
                type: object
                x-kubernetes-validations:
                - message: the failurePolicy field is immutable
                  rule: self == oldSelf
              fromClusterSelector:
                additionalProperties:
                  type: string
                description: |-
                  A label matcher that select the clusters to migrate workloads from. For each selected from
                  cluster, KubeFleet will migrate the workloads, as selected by the workloadPlacementSelectors field above,
                  to a corresponding to cluster, as selected by the toClusterSelectors field below. KubeFleet
                  may request to create a new cluster if an appropriate target to cluster cannot be found.

                  This field is immutable after creation.
                type: object
                x-kubernetes-validations:
                - message: the fromClusterSelector field is immutable
                  rule: self == oldSelf
              maxConcurrency:
                default: 1
                description: |-
                  The maximum number of workloads to migrate in parallel.

                  The default value is 1.

                  This field is immutable after creation.
                format: int32
                maximum: 10
                minimum: 1
                type: integer
                x-kubernetes-validations:
                - message: the maxConcurrency field is immutable
                  rule: self == oldSelf
              mode:
                default: SurgeFirst
                description: |-
                  How KubeFleet should handle this migration.

                  The available options are:
                  * SurgeFirst: for each workload to migrate, KubeFleet will add the workload first to a to cluster,
                    wait until it becomes available, then remove it from the from cluster. If a to cluster cannot
                    be found, KubeFleet will send a cluster request and wait until a to cluster becomes available before
                    proceeding with the migration; during this period the workload will continue to run on the from cluster.
                  * DrainFirst: for each workload to migrate, KubeFleet will first remove the workload from the from
                    cluster, then add it to a to cluster. If a to cluster cannot be found, KubeFleet will send a cluster
                    request; during this period the from cluster will not be running the workload anymore.

                  The default mode is SurgeFirst.

                  This field is immutable after creation.
                enum:
                - SurgeFirst
                - DrainFirst
                type: string
                x-kubernetes-validations:
                - message: the mode field is immutable
                  rule: self == oldSelf
              rollback:
                default: false
                description: |-
                  Whether to roll back the migration attempt. If set to true, KubeFleet will move workloads
                  back from the to clusters to the from clusters. Once the rollback starts, it cannot be
                  cancelled; to start the migration again, delete and re-create a workload migration request as
                  needed.

                  During the rollback process, KbueFleet will use the same mode and maxConcurrency settings as
                  specified in this request; however, the failure policy will cease to apply. KubeFleet will
                  move back as many workloads as possible, even if the from clusters fail to run them
                  for some reason.

                  The field always starts with false and can only be set to true once. After that it cannot be changed anymore.
                type: boolean
                x-kubernetes-validations:
                - message: the rollback field cannot be reset to false once set to
                    true
                  rule: '!oldSelf || self'
              toClusterSelector:
                additionalProperties:
                  type: string
                description: |-
                  A label matcher that select the clusters to which workloads are migrated. KubeFleet may
                  use this selector to request a new cluster if an appropriate target to cluster cannot be found.

                  If the selector is unset, any cluster can be a target.

                  This field is immutable after creation.
                type: object
                x-kubernetes-validations:
                - message: the toClusterSelector field is immutable
                  rule: self == oldSelf
              workloadPlacementSelectors:
                description: |-
                  A list of label matchers that select the workload placements to migrate; KubeFleet will migrate
                  them if they are placed on the from clusters as selected by the fromClusterSelectors field
                  below.

                  These selectors are OR'd.

                  If the list is empty, all workload placements on the from cluster are selected. This is best used to
                  migrate all workload placements from one cluster to another, so as to, for example, decommission a cluster.

                  This field is immutable after creation.
                items:
                  additionalProperties:
                    type: string
                  type: object
                maxItems: 5
                type: array
                x-kubernetes-validations:
                - message: the workloadPlacementSelectors field is immutable
                  rule: self == oldSelf
            required:
            - failurePolicy
            - fromClusterSelector
            type: object
          status:
            description: The status of a workload migration request.
            properties:
              conditions:
                description: A list of observed conditions of this workload migration
                  request.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              migrationAttempts:
                description: A list of migration attempts that KubeFleet will perform
                  for this migration request.
                items:
                  properties:
                    conditions:
                      description: A list of observed conditions of this migration
                        attempt.
                      items:
                        description: Condition contains details for one aspect of
                          the current state of this API Resource.
                        properties:
                          lastTransitionTime:
                            description: |-
                              lastTransitionTime is the last time the condition transitioned from one status to another.
                              This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                            format: date-time
                            type: string
                          message:
                            description: |-
                              message is a human readable message indicating details about the transition.
                              This may be an empty string.
                            maxLength: 32768
                            type: string
                          observedGeneration:
                            description: |-
                              observedGeneration represents the .metadata.generation that the condition was set based upon.
                              For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                              with respect to the current state of the instance.
                            format: int64
                            minimum: 0
                            type: integer
                          reason:
                            description: |-
                              reason contains a programmatic identifier indicating the reason for the condition's last transition.
                              Producers of specific condition types may define expected values and meanings for this field,
                              and whether the values are considered a guaranteed API.
                              The value should be a CamelCase string.
                              This field may not be empty.
                            maxLength: 1024
                            minLength: 1
                            pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                            type: string
                          status:
                            description: status of the condition, one of True, False,
                              Unknown.
                            enum:
                            - "True"
                            - "False"
                            - Unknown
                            type: string
                          type:
                            description: type of condition in CamelCase or in foo.example.com/CamelCase.
                            maxLength: 316
                            pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                            type: string
                        required:
                        - lastTransitionTime
                        - message
                        - reason
                        - status
                        - type
                        type: object
                      type: array
                    fromClusterName:
                      description: The from cluster of this migration attempt.
                      type: string
                    toClusterName:
                      description: |-
                        The to cluster of this migration attempt. If unset, the to cluster is not determined yet,
                        and KubeFleet may have to request a new cluster to fulfill the migration attempt.
                      type: string
                    toClusterRequestName:
                      description: The cluster request created for this migration
                        attempt, if any.
                      type: string
                    workloadPlacementRef:
                      description: The reference to the workload placement involved
                        in this migration attempt.
                      properties:
                        apiGroup:
                          type: string
                        apiVersion:
                          type: string
                        kind:
                          type: string
                        name:
                          description: The name of the referenced object.
                          type: string
                        namespace:
                          description: The namespace of the referenced object.
                          type: string
                        resource:
                          type: string
                      required:
                      - apiVersion
                      - kind
                      - name
                      - namespace
                      - resource
                      type: object
                    workloadResourceBindingRef:
                      description: The reference to workload resource cluster binding
                        involved in this migration attempt.
                      properties:
                        apiGroup:
                          type: string
                        apiVersion:
                          type: string
                        kind:
                          type: string
                        name:
                          description: The name of the referenced object.
                          type: string
                        namespace:
                          description: The namespace of the referenced object.
                          type: string
                        resource:
                          type: string
                      required:
                      - apiVersion
                      - kind
                      - name
                      - namespace
                      - resource
                      type: object
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
