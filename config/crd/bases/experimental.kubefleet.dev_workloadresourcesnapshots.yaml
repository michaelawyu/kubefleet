---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: workloadresourcesnapshots.experimental.kubefleet.dev
spec:
  group: experimental.kubefleet.dev
  names:
    categories:
    - kubefleet
    - kubefleet-experimental
    kind: WorkloadResourceSnapshot
    listKind: WorkloadResourceSnapshotList
    plural: workloadresourcesnapshots
    shortNames:
    - wkrs
    singular: workloadresourcesnapshot
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: |-
          WorkloadResourceSnapshot is the KubeFleet API that captures the resources
          (e.g., pod templates, configmaps, secrets, etc.) of a workload as seen on the hub cluster
          at a specific point in time. It can be referenced by other KubeFleet APIs
          to enable consistent rollouts of resources across multiple member clusters in the fleet.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: The spec of a workload resource snapshot.
            properties:
              additionalResources:
                description: The manifests of the additional resources for a workload,
                  e.g., configmaps, secrets, etc.
                items:
                  properties:
                    identifier:
                      description: The identifier of the resource represented by the
                        manifest.
                      properties:
                        apiGroup:
                          type: string
                        apiVersion:
                          type: string
                        kind:
                          type: string
                        name:
                          description: The name of the referenced object.
                          type: string
                        resource:
                          type: string
                      required:
                      - apiVersion
                      - kind
                      - name
                      - resource
                      type: object
                    manifest:
                      description: The manifest of the resource. It should be a Kubernetes
                        object in YAML or JSON format.
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                  required:
                  - identifier
                  - manifest
                  type: object
                type: array
              workload:
                description: The manifest of the workload object.
                properties:
                  identifier:
                    description: The identifier of the resource represented by the
                      manifest.
                    properties:
                      apiGroup:
                        type: string
                      apiVersion:
                        type: string
                      kind:
                        type: string
                      name:
                        description: The name of the referenced object.
                        type: string
                      resource:
                        type: string
                    required:
                    - apiVersion
                    - kind
                    - name
                    - resource
                    type: object
                  manifest:
                    description: The manifest of the resource. It should be a Kubernetes
                      object in YAML or JSON format.
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                required:
                - identifier
                - manifest
                type: object
            type: object
        type: object
    served: true
    storage: true
