---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.0
  name: dynamicresourceslices.placement.kubernetes-fleet.io
spec:
  group: placement.kubernetes-fleet.io
  names:
    categories:
    - fleet
    - fleet-placement
    kind: DynamicResourceSlice
    listKind: DynamicResourceSliceList
    plural: dynamicresourceslices
    singular: dynamicresourceslice
  scope: Cluster
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec is the desired configuration of a DynamicResourceSlice.
            properties:
              clusterName:
                description: |-
                  ClusterName is the name of the cluster where the resource slice is available.
                  TO-DO (chenyu1): for simplicity reasons, at this stage the system assumes resources are always
                  cluster-local; switch to a more flexible model that allows non-local resources as needed.
                type: string
              provisioner:
                description: Provisioner is the name of the source that prepares this
                  resource slice.
                type: string
              resources:
                description: Resources are the list of dynamic resources specified
                  by this slice.
                items:
                  properties:
                    attributes:
                      additionalProperties:
                        properties:
                          typeHint:
                            default: String
                            enum:
                            - String
                            type: string
                          value:
                            type: string
                        required:
                        - typeHint
                        - value
                        type: object
                      type: object
                    capacity:
                      additionalProperties:
                        properties:
                          quantity:
                            anyOf:
                            - type: integer
                            - type: string
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                        type: object
                      maxProperties: 100
                      minProperties: 1
                      type: object
                    name:
                      description: Name is the name of the dynamic resource. It must
                        be a DNS label.
                      type: string
                  required:
                  - capacity
                  - name
                  type: object
                maxItems: 100
                minItems: 1
                type: array
            required:
            - clusterName
            - provisioner
            - resources
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
