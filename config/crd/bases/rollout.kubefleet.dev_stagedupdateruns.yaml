---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: stagedupdateruns.rollout.kubefleet.dev
spec:
  group: rollout.kubefleet.dev
  names:
    categories:
    - kubefleet
    - kubefleet-rollout
    kind: StagedUpdateRun
    listKind: StagedUpdateRunList
    plural: stagedupdateruns
    singular: stagedupdaterun
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          StagedUpdateRun is the KubeFleet API that enables users to roll out resource changes for a placement policy
          in a staged manner, where clusters are grouped into stages and KubeFleet applies resource changes to each stage
          sequentially.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: The specification of the StagedUpdateRun API object.
            properties:
              failurePolicy:
                description: |-
                  The failure policy of the staged update run. It helps KubeFleet determine when to stop the staged update run
                  when there are too many failures, so as to keep the impact radius under control.
                properties:
                  maxFailureCount:
                    default: 1
                    description: |-
                      The maximum number of failures (e.g., clusters where the rollout fails) before KubeFleet stops the staged update run.

                      The default value is 1.
                    format: int32
                    minimum: 1
                    type: integer
                  maxWaitTimePerClusterMinutes:
                    default: 30
                    description: |-
                      The maximum time to wait for a rollout to a cluster to complete before KubeFleet considers the rollout to have
                      failed for the cluster.

                      The default value is 30 minutes.
                    format: int32
                    minimum: 1
                    type: integer
                type: object
                x-kubernetes-validations:
                - message: failurePolicy is immutable
                  rule: self == oldSelf
              placementPolicyName:
                description: |-
                  The name of the placement policy that the StagedUpdateRun API object is associated with, i.e.,
                  the name of the placement policy where resource changes are rolled out.
                type: string
                x-kubernetes-validations:
                - message: placementPolicyName is immutable
                  rule: self == oldSelf
              resourceSnapshotName:
                description: |-
                  The name of the resource snapshot that the StagedUpdateRun API object is associated with, i.e.,
                  the name of the resource snapshot where resource changes are introduced.

                  If left empty, KubeFleet will request a new resource snapshot to be created based on the current state
                  of selected resources in the placement policy (if applicable).
                maxLength: 253
                type: string
                x-kubernetes-validations:
                - message: resourceSnapshotName is immutable
                  rule: self == oldSelf
              stagedUpdateStrategyName:
                description: |-
                  The name of the staged update strategy that the StagedUpdateRun API object is associated with, i.e.,
                  the name of the staged update strategy that organizes clusters into stages and defines how the rollout is executed.
                type: string
                x-kubernetes-validations:
                - message: stagedUpdateStrategyName is immutable
                  rule: self == oldSelf
              suspended:
                default: false
                description: |-
                  Whether the staged update run is suspended. If set to true, KubeFleet will pause the rollout.

                  If the staged update run is created with this field set to true, KubeFleet will initialize the staged update run,
                  i.e., decide on the resource snapshot to roll out and determine the stages and the clusters in each stage,
                  but will not start rolling out resource changes until the field is set back to false.
                type: boolean
            required:
            - placementPolicyName
            - stagedUpdateStrategyName
            type: object
            x-kubernetes-validations:
            - message: resourceSnapshotName cannot be added or removed after creation
              rule: has(self.resourceSnapshotName) == has(oldSelf.resourceSnapshotName)
            - message: failurePolicy cannot be added or removed after creation
              rule: has(self.failurePolicy) == has(oldSelf.failurePolicy)
          status:
            description: The observed status of the StagedUpdateRun API object.
            properties:
              conditions:
                description: A list of observed conditions of the staged update run.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              resourceSnapshotNameToRollout:
                description: |-
                  The name of the resource snapshot that is being rolled out in the staged update run.

                  If the staged update run is created with the resourceSnapshotName field in the spec set to a specific value,
                  this field will be set to the same value; otherwise, KubeFleet will request a new resource snapshot to be created (if applicable)
                  and populate this field with the name of the newly created resource snapshot.
                type: string
              stages:
                description: The status of each stage in the staged update run, including
                  the clusters in each stage and their rollout status.
                items:
                  properties:
                    afterStageTasks:
                      description: The list of tasks to be executed after completing
                        the rollout in the stage, and their execution status.
                      items:
                        properties:
                          approvalRequestName:
                            description: |-
                              The name of the approval request that is created for the stage task.

                              This field is only set when the task type is Approval.
                            type: string
                          conditions:
                            description: A list of observed conditions about the progress
                              of the stage task.
                            items:
                              description: Condition contains details for one aspect
                                of the current state of this API Resource.
                              properties:
                                lastTransitionTime:
                                  description: |-
                                    lastTransitionTime is the last time the condition transitioned from one status to another.
                                    This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                                  format: date-time
                                  type: string
                                message:
                                  description: |-
                                    message is a human readable message indicating details about the transition.
                                    This may be an empty string.
                                  maxLength: 32768
                                  type: string
                                observedGeneration:
                                  description: |-
                                    observedGeneration represents the .metadata.generation that the condition was set based upon.
                                    For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                                    with respect to the current state of the instance.
                                  format: int64
                                  minimum: 0
                                  type: integer
                                reason:
                                  description: |-
                                    reason contains a programmatic identifier indicating the reason for the condition's last transition.
                                    Producers of specific condition types may define expected values and meanings for this field,
                                    and whether the values are considered a guaranteed API.
                                    The value should be a CamelCase string.
                                    This field may not be empty.
                                  maxLength: 1024
                                  minLength: 1
                                  pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                                  type: string
                                status:
                                  description: status of the condition, one of True,
                                    False, Unknown.
                                  enum:
                                  - "True"
                                  - "False"
                                  - Unknown
                                  type: string
                                type:
                                  description: type of condition in CamelCase or in
                                    foo.example.com/CamelCase.
                                  maxLength: 316
                                  pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                                  type: string
                              required:
                              - lastTransitionTime
                              - message
                              - reason
                              - status
                              - type
                              type: object
                            type: array
                            x-kubernetes-list-map-keys:
                            - type
                            x-kubernetes-list-type: map
                          type:
                            description: The type of the stage task.
                            enum:
                            - TimedWait
                            - Approval
                            type: string
                          waitTime:
                            description: The time to wait in a TimedWait task.
                            type: string
                        required:
                        - type
                        type: object
                      maxItems: 2
                      type: array
                    beforeStageTasks:
                      description: The list of tasks to be executed before starting
                        the rollout in the stage, and their execution status.
                      items:
                        properties:
                          approvalRequestName:
                            description: |-
                              The name of the approval request that is created for the stage task.

                              This field is only set when the task type is Approval.
                            type: string
                          conditions:
                            description: A list of observed conditions about the progress
                              of the stage task.
                            items:
                              description: Condition contains details for one aspect
                                of the current state of this API Resource.
                              properties:
                                lastTransitionTime:
                                  description: |-
                                    lastTransitionTime is the last time the condition transitioned from one status to another.
                                    This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                                  format: date-time
                                  type: string
                                message:
                                  description: |-
                                    message is a human readable message indicating details about the transition.
                                    This may be an empty string.
                                  maxLength: 32768
                                  type: string
                                observedGeneration:
                                  description: |-
                                    observedGeneration represents the .metadata.generation that the condition was set based upon.
                                    For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                                    with respect to the current state of the instance.
                                  format: int64
                                  minimum: 0
                                  type: integer
                                reason:
                                  description: |-
                                    reason contains a programmatic identifier indicating the reason for the condition's last transition.
                                    Producers of specific condition types may define expected values and meanings for this field,
                                    and whether the values are considered a guaranteed API.
                                    The value should be a CamelCase string.
                                    This field may not be empty.
                                  maxLength: 1024
                                  minLength: 1
                                  pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                                  type: string
                                status:
                                  description: status of the condition, one of True,
                                    False, Unknown.
                                  enum:
                                  - "True"
                                  - "False"
                                  - Unknown
                                  type: string
                                type:
                                  description: type of condition in CamelCase or in
                                    foo.example.com/CamelCase.
                                  maxLength: 316
                                  pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                                  type: string
                              required:
                              - lastTransitionTime
                              - message
                              - reason
                              - status
                              - type
                              type: object
                            type: array
                            x-kubernetes-list-map-keys:
                            - type
                            x-kubernetes-list-type: map
                          type:
                            description: The type of the stage task.
                            enum:
                            - TimedWait
                            - Approval
                            type: string
                          waitTime:
                            description: The time to wait in a TimedWait task.
                            type: string
                        required:
                        - type
                        type: object
                      maxItems: 1
                      type: array
                    clusters:
                      description: The list of clusters featured in the stage, and
                        their individual rollout status.
                      items:
                        properties:
                          clusterName:
                            description: The name of the cluster.
                            type: string
                          conditions:
                            description: A list of observed conditions about the rollout
                              progress of the cluster.
                            items:
                              description: Condition contains details for one aspect
                                of the current state of this API Resource.
                              properties:
                                lastTransitionTime:
                                  description: |-
                                    lastTransitionTime is the last time the condition transitioned from one status to another.
                                    This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                                  format: date-time
                                  type: string
                                message:
                                  description: |-
                                    message is a human readable message indicating details about the transition.
                                    This may be an empty string.
                                  maxLength: 32768
                                  type: string
                                observedGeneration:
                                  description: |-
                                    observedGeneration represents the .metadata.generation that the condition was set based upon.
                                    For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                                    with respect to the current state of the instance.
                                  format: int64
                                  minimum: 0
                                  type: integer
                                reason:
                                  description: |-
                                    reason contains a programmatic identifier indicating the reason for the condition's last transition.
                                    Producers of specific condition types may define expected values and meanings for this field,
                                    and whether the values are considered a guaranteed API.
                                    The value should be a CamelCase string.
                                    This field may not be empty.
                                  maxLength: 1024
                                  minLength: 1
                                  pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                                  type: string
                                status:
                                  description: status of the condition, one of True,
                                    False, Unknown.
                                  enum:
                                  - "True"
                                  - "False"
                                  - Unknown
                                  type: string
                                type:
                                  description: type of condition in CamelCase or in
                                    foo.example.com/CamelCase.
                                  maxLength: 316
                                  pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                                  type: string
                              required:
                              - lastTransitionTime
                              - message
                              - reason
                              - status
                              - type
                              type: object
                            type: array
                            x-kubernetes-list-map-keys:
                            - type
                            x-kubernetes-list-type: map
                          placementBindingName:
                            description: The name of the placement binding.
                            type: string
                        required:
                        - clusterName
                        - placementBindingName
                        type: object
                      type: array
                    completedTimestamp:
                      description: The timestamp when the rollout in the stage completed.
                        If unset, the rollout has not completed in the stage yet.
                      format: date-time
                      type: string
                    conditions:
                      description: A list of observed conditions about the rollout
                        progress in the stage.
                      items:
                        description: Condition contains details for one aspect of
                          the current state of this API Resource.
                        properties:
                          lastTransitionTime:
                            description: |-
                              lastTransitionTime is the last time the condition transitioned from one status to another.
                              This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                            format: date-time
                            type: string
                          message:
                            description: |-
                              message is a human readable message indicating details about the transition.
                              This may be an empty string.
                            maxLength: 32768
                            type: string
                          observedGeneration:
                            description: |-
                              observedGeneration represents the .metadata.generation that the condition was set based upon.
                              For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                              with respect to the current state of the instance.
                            format: int64
                            minimum: 0
                            type: integer
                          reason:
                            description: |-
                              reason contains a programmatic identifier indicating the reason for the condition's last transition.
                              Producers of specific condition types may define expected values and meanings for this field,
                              and whether the values are considered a guaranteed API.
                              The value should be a CamelCase string.
                              This field may not be empty.
                            maxLength: 1024
                            minLength: 1
                            pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                            type: string
                          status:
                            description: status of the condition, one of True, False,
                              Unknown.
                            enum:
                            - "True"
                            - "False"
                            - Unknown
                            type: string
                          type:
                            description: type of condition in CamelCase or in foo.example.com/CamelCase.
                            maxLength: 316
                            pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                            type: string
                        required:
                        - lastTransitionTime
                        - message
                        - reason
                        - status
                        - type
                        type: object
                      type: array
                      x-kubernetes-list-map-keys:
                      - type
                      x-kubernetes-list-type: map
                    maxConcurrency:
                      description: The (resolved) maximum number of clusters that
                        can be updated concurrently within this stage.
                      format: int32
                      type: integer
                    stageName:
                      description: The name of the stage.
                      maxLength: 63
                      pattern: ^[a-z0-9]+$
                      type: string
                    startedTimestamp:
                      description: The timestamp when the rollout in the stage started.
                        If unset, the rollout has not started in the stage yet.
                      format: date-time
                      type: string
                  required:
                  - stageName
                  type: object
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
