apiVersion: apps/v1
kind: Deployment
metadata:
  name: pause-5
  namespace: work
spec:
  replicas: 0
  selector:
    matchLabels:
      app: pause-5
  template:
    metadata:
      labels:
        app: pause-5
    spec:
      containers:
        - name: pause
          image: k8s.gcr.io/pause:3.2
          ports:
            - containerPort: 80
