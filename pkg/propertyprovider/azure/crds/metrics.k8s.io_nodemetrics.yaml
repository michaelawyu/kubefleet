---
# Generated by controller-gen from the metrics API repository.
# Used for testing purposes only; this API is normally implemented as an API aggregation
# layer rather than a CRD in a Kubernetes cluster.
# See https://github.com/kubernetes/metrics/tree/master/pkg/apis/metrics/v1beta1.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.0
    # This field is needed for all APIs under the k8s.io group. The value is
    # a placeholder.
    # If this CRD YAML file is re-gen'd, add this annotation manually, otherwise
    # the API server will reject the installation.
    api-approved.kubernetes.io: "https://github.com/kubernetes/kubernetes/pull/1"
  name: nodemetrics.metrics.k8s.io
spec:
  group: metrics.k8s.io
  names:
    kind: NodeMetrics
    listKind: NodeMetricsList
    plural: nodemetrics
    singular: nodemetrics
  scope: Cluster
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: NodeMetrics sets resource usage metrics of a node.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          timestamp:
            description: |-
              The following fields define time interval from which metrics were
              collected from the interval [Timestamp-Window, Timestamp].
            format: date-time
            type: string
          usage:
            additionalProperties:
              anyOf:
              - type: integer
              - type: string
              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
              x-kubernetes-int-or-string: true
            description: The memory usage is the memory working set.
            type: object
          window:
            type: string
        required:
        - timestamp
        - usage
        - window
        type: object
    served: true
    storage: true
